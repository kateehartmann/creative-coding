let ripple;

function setup() {
  createCanvas(800, 800);
}

function draw() {
  background(220);
  function mousePressed(){

  }
  class Ripple {
    constructor(x,y){
      this.x = x,
      this.y = y,
      this.diameter = 0
    }
  }
  for (let i = 0; i < ripples.length; i++){
    ripples[i].draw();
  }
}